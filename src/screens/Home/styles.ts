import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  text: {
    marginTop: 40,
    fontSize: 20,
  },
  menu: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-around",
  },
});
